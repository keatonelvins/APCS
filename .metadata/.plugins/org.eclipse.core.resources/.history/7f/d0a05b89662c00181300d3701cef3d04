package lab18d;

import java.io.File;
import java.io.IOException;
import java.util.Scanner;
import java.util.ArrayList;
import java.util.Arrays;
import static java.lang.System.*;

public class Lab18dArray
{
	public static void main( String args[] ) throws IOException
	{
		Scanner file = new Scanner(new File("H:\\APCS\\Unit12\\Unit12-Assignments\\lab18d\\lab18d.dat"));

		int size = file.nextInt();
		file.nextLine();
		
		Word[] words = new Word[size];
		
		for(int i = 0; i < size; i++){
			words[i] = new Word(file.next());
		}
		
		int min = 0;
		for(int i = 0; i < words.length; i++){
			for(int j = i; j < words.length; j++){
				if(words[i].compareTo(words[min]) == -1)
					min = i;
			}
			Word temp = new Word(words[i].toString());
			words[i] = new Word(words[min].toString());
			words[min] = new Word(temp.toString());
		}
		
		out.print(words.toString());
	}
}