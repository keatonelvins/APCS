//© A+ Computer Science  -  www.apluscompsci.com
//Name -
//Date -
//Class -
//Lab  -
package lab18d;

import java.io.File;
import java.io.IOException;
import java.util.Scanner;
import java.util.ArrayList;
import java.util.Arrays;
import static java.lang.System.*;

public class Lab18d
{
	public static void main( String args[] ) throws IOException
	{
		Scanner file = new Scanner(new File("H:\\APCS\\Unit12\\Unit12-Assignments\\lab18d\\lab18d.dat"));

		int size = file.nextInt();
		file.nextLine();
		
		ArrayList<Word> words = new ArrayList<Word>(size);
		
		for(int i = 0; i < size; i++){
			words.add(new Word(file.next()));
		}
		
		System.out.println(words.toString());
		
		sort(words);
	}
	
	public static void sort(ArrayList<Word> wordList)
	{
		int min = 0;
		int spot = 0;
		for(int i = 0; i < wordList.size(); i++){
			for(int j = i; j < wordList.size(); j++){
				min = j;
				if(wordList.get(j).toString().length() < wordList.get(min).toString().length())
					min = j;
			}
			Word temp = new Word(wordList.get(spot).toString());
			System.out.println(temp.toString());
			System.out.println(min);
			wordList.set(spot, wordList.get(min));
			wordList.set(min, temp);
			spot++;
			System.out.println(wordList.toString());
		}
	}
}

/*
for(int i = 0; i < wordList.size(); i++){
			for(int j = 0; j < wordList.size(); j++){
				if(wordList.get(j).toString().length() < wordList.get(min).toString().length())
					min = j;
			}
			wordList.add(new Word(wordList.get(min).toString()));
		}
		
		
		int min = 0;
		int stop = wordList.size()-1;
		for(int i = 0; i < 10; i++){
			for(int j = 0; j < stop-1; j++){
				if(wordList.get(j).toString().length() < wordList.get(min).toString().length())
					min = j;
			}
			wordList.add(new Word(wordList.get(min).toString()));
			wordList.remove(min);
			stop--;
			System.out.println("\n" + stop);
			System.out.println(wordList.toString());
		}
*/